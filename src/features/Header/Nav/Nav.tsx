import styles from './Nav.module.css';

export function Nav() {
  return (
    <nav className={styles.nav}>
      <span>nav</span>
      <span>nav</span>
      <span>nav</span>
      <span>nav</span>
    </nav>
  );
}

export default Nav;